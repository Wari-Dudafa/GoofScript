int x : 0;
int y : 100;
int z : 1000;

str my_name : john;
str your_name : john;
str same_name_message : We have the same name!!;

str bigger_y : y is bigger than x;
str bigger_y : y is bigger than x;

if y < x;
  print bigger_y;
else;
  print bigger_x;
endif;

if y > x;
  print bigger_y;
endif;

if z > x;
  print z;
endif;

if my_name = your_name;
  print same_name_message;
endif;